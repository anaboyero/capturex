services:
  capturex:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: capturex-app
    ports:
      - "8080:8080"
    volumes:
      - capturex-data:/app/data
    restart: unless-stopped

volumes:
  capturex-data:
